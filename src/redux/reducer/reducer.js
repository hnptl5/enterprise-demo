/* import * as actionTypes from '../actions/actionConstants';

const initialState = {
  count: 0
};

export const reducerName = (state = initialState, action) => {
  switch (action.type) {
    case actionTypes.INCRMENT:
      return {
        ...state,
        count: state.count + 1
      };
    case actionTypes.DECREMENT:
      return {
        ...state,
        count: state.count - 1
      };
    default:
      return state;
  }
};
 */